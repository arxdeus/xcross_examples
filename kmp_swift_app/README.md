# KMP Swift host smoke app

Minimal Kotlin Multiplatform framework consumed by a SwiftUI `@main` iOS host.

```sh
xcross compose setup
xcross compose build
xcross compose run -d <device>
```

The build output is `build/xcross-ios/KmpSwiftApp.app/Runner`.
