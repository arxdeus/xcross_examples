# Compose UIKit smoke app

Minimal Compose Multiplatform project whose iOS entry point is a Kotlin `ComposeUIViewController`.

```sh
xcross compose setup
xcross compose build
xcross compose run -d <device>
```

The build output is `build/xcross-ios/ComposeApp.app/Runner`.
